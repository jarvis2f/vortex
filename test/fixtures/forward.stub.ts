import { Forward, ForwardMethod, ForwardStatus } from ".prisma/client";
import { agentStub } from "./agent.stub";
import { userStub } from "./user.stub";

export const forwardStub = {
  forward: Object.freeze<Forward>({
    id: "forward-id",
    agentId: agentStub.agent.id,
    createdById: userStub.user.id,
    method: ForwardMethod.IPTABLES,
    status: ForwardStatus.RUNNING,
    options: {},
    agentPort: 1080,
    targetPort: 1090,
    target: "0.0.0.0",
    targetType: "EXTERNAL",
    usedTraffic: 0,
    download: 0,
    upload: 0,
    remark: "remark",
    createdAt: new Date(),
    updatedAt: new Date(),
    deleted: false,
  }),
  forward2agent: Object.freeze<Forward>({
    id: "forward-id",
    agentId: agentStub.agent.id,
    createdById: userStub.user.id,
    method: ForwardMethod.IPTABLES,
    status: ForwardStatus.RUNNING,
    options: {
      channel: "ws",
      listen: "ws",
      forward: "relay+ws",
    },
    agentPort: 1080,
    targetPort: 1090,
    target: agentStub.agent2.id,
    targetType: "AGENT",
    usedTraffic: 0,
    download: 0,
    upload: 0,
    remark: "remark",
    createdAt: new Date(),
    updatedAt: new Date(),
    deleted: false,
  }),
  forward2External: Object.freeze<Forward>({
    id: "forward-id",
    agentId: agentStub.agent.id,
    createdById: userStub.user.id,
    method: ForwardMethod.GOST,
    status: ForwardStatus.RUNNING,
    options: {
      listen: "tcp",
      forward: "tcp",
    },
    agentPort: 1080,
    targetPort: 1090,
    target: "0.0.0.0",
    targetType: "EXTERNAL",
    usedTraffic: 0,
    download: 0,
    upload: 0,
    remark: "remark",
    createdAt: new Date(),
    updatedAt: new Date(),
    deleted: false,
  }),
  forward2ExternalNoneOption: Object.freeze<Forward>({
    id: "forward-id",
    agentId: agentStub.agent.id,
    createdById: userStub.user.id,
    method: ForwardMethod.GOST,
    status: ForwardStatus.RUNNING,
    options: {},
    agentPort: 1080,
    targetPort: 1090,
    target: "0.0.0.0",
    targetType: "EXTERNAL",
    usedTraffic: 0,
    download: 0,
    upload: 0,
    remark: "remark",
    createdAt: new Date(),
    updatedAt: new Date(),
    deleted: false,
  }),
};
